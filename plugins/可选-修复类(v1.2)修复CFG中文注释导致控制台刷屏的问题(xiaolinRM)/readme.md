# 可选-修复类(v1.2)修复CFG中文注释导致控制台刷屏的问题(xiaolinRM)

## 简介

Fix non ASCII characters in cfg file that cannot be executed.

## 配置指令 (ConVar)

- `l4d2_parseline_fix_blockcomment` 默认值: `1` - Interpret /* */ as a comment block
