# 自选-被特感控制时等禁止闲置和防止闲置加入提示刷屏(v1.0.0)

## 简介

多种状态禁止使用闲置.

## 配置指令 (ConVar)

- `l4d2_enabled_away_from_server` 默认值: `1` - 启用被特感控制,发射榴弹,投掷燃烧瓶时禁止使用休息? 0=禁用, 1=启用.
- `l4d2_enabled_away_timer_keyboard` 默认值: `8.0` - 设置多少秒后才能再次使用休息.
- `l4d2_enabled_away_timer_specnext` 默认值: `3.0` - 设置多少秒后才能再加入幸存者.
