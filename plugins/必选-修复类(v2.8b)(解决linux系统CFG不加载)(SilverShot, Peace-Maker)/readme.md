# 必选-修复类(v2.8b)(解决linux系统CFG不加载)(SilverShot, Peace-Maker)

## 简介

Fixes the 'Cbuf_AddText: buffer overflow' console error on servers, which causes ConVars to use their default value.

## 可用指令

- `sm_cvar_test` [管理员指令] (权限: ADMFLAG_ROOT)
