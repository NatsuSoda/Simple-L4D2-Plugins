# 必选-功能类插件(WeaponHandling)(v1.0.6)(Lux)

## 简介

Weapon Handling API for guns and melee weapons in left 4 dead

## 配置指令 (ConVar)

- `wh_double_pistol_cycle_rate` 默认值: `0`
- `wh_use_incap_cycle_cvar` 默认值: `1` - 1 = (use \
- `wh_deploy_animation_speed` 默认值: `-1` - 1 = (match deploy animation speed to the \
