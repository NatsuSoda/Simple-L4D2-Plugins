# 自选-动态大厅(二选一)(v2.0.4)(Downtown1, Anime4000, sorallll, HatsuneImagine)

## 简介

public Plugin myinfo = {

## 可用指令

- `sm_unreserve` [管理员指令] (权限: ADMFLAG_BAN)
- `sm_reserve` [管理员指令] (权限: ADMFLAG_BAN)

## 配置指令 (ConVar)

- `l4d_unreserve_full` 默认值: `1` - Automatically unreserve server after a full lobby joins
